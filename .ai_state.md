# Project Objective: Scholarly static gallery for A. A. Zaliznyak video records

## ➡️ Next Steps (Queue)

- [ ] Deploy the reviewed release branch and run the documented post-deploy URL checks.

## 🚧 Current Work-In-Progress (WIP)

- [x] Reworked `scripts/build_site.py` around the public v2 contract with a checked-in v1 migration fallback.
- [x] **Scholarly gallery v2 (Codex, 03-08-2026):** generated from the BookIndex public export in isolated branch `codex/zaliz-video-gallery-v2`.

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)

- `file://` YouTube embeds produce Error 153 because there is no HTTP referrer; supported local preview uses `python -m http.server`.
- Preserve `/video/<youtube-id>/` as noindex alias pages; never renumber frozen accessions.
- Normal builds are deliberately offline and fail release validation if `assets/thumbs/<accession>.jpg` is missing. Network fetching requires the explicit `--fetch-thumbnails` flag.

## ✅ Completed (Recent only)

- [x] H2135 shipped the initial 175-page static index on 01-08-2026.
- [x] Added the v2 normalization, dual JSON exports, canonical and alias renderers, URL-synced filters, click-to-load player, SEO metadata, and focused unit tests on 03-08-2026.
- [x] Generated 175 canonical pages, 175 aliases and 175 local thumbnails from the frozen accession registry.
